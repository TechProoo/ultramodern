# Copy to .env and fill in the real values (never commit .env)

# Supabase Postgres — transaction-mode pooler (app queries)
DATABASE_URL="postgresql://postgres.jynapvjebiuedqrpmtvj:[YOUR-PASSWORD]@aws-0-eu-north-1.pooler.supabase.com:6543/postgres?pgbouncer=true"
# Supabase Postgres — session-mode pooler (migrations)
DIRECT_URL="postgresql://postgres.jynapvjebiuedqrpmtvj:[YOUR-PASSWORD]@aws-0-eu-north-1.pooler.supabase.com:5432/postgres"

# Production frontend origin(s) allowed by CORS — comma-separated.
# localhost dev origins are always allowed automatically.
CORS_ORIGIN="https://your-site.netlify.app"

# PORT is injected by Railway in production; defaults to 3000 locally.
# PORT=3000
